services:
  - type: web
    name: sw-monthly-golf
    env: node
    nodeVersion: 20
    buildCommand: npm run build
    startCommand: npm run db:push && npm start
    envVars:
      - key: DATABASE_URL
        sync: false
